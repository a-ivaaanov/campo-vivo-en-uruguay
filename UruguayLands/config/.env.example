# Настройки приложения UruguayLands
# Переименуйте этот файл в .env для активации настроек

# Общие настройки
LOG_LEVEL=INFO                # Уровень логирования (INFO, DEBUG, WARNING, ERROR)
CHECK_INTERVAL_MINUTES=60     # Интервал между проверками в минутах
PARSERS_TO_RUN=all            # Парсеры для запуска (all, mercadolibre, infocasas)

# Настройки парсинга
MAX_PAGES_PER_RUN=2           # Макс. кол-во страниц для обработки каждым парсером
HEADLESS_MODE=True            # Запускать браузер в фоновом режиме (true/false)

# Пути к файлам
DATA_DIR=data                 # Директория для сохранения данных
LOGS_DIR=logs                 # Директория для логов

# Настройки Telegram
BOT_TOKEN=your_telegram_bot_token_here  # Получите токен у @BotFather
CHAT_ID=your_telegram_chat_id_here      # ID канала или группы для отправки сообщений

# Настройки запуска
TELEGRAM_DELAY_SECONDS=10  # Задержка между отправками сообщений в Telegram (в секундах)

# Настройки логирования
# LOG_LEVEL=INFO             # DEBUG, INFO, WARNING, ERROR или CRITICAL

# Настройки прокси (если необходимо)
# PROXY_FILE_PATH=config/proxies.txt  # Путь к файлу со списком прокси (каждый прокси на новой строке)
                                     # Формат: http://user:pass@host:port или http://host:port 

# --- Настройки Selenium ---
MAX_RETRIES=3
REQUEST_DELAY_MIN=3
REQUEST_DELAY_MAX=8

# --- Настройки Smartproxy ---
SMARTPROXY_SERVER=uy.smartproxy.com:15001
SMARTPROXY_USER=spgai22txz
SMARTPROXY_PASSWORD=jtx6i24Jpb~eweNw2eo
# Дополнительные серверы для ротации прокси (разделены запятыми)
ALTERNATIVE_PROXY_SERVERS=us.smartproxy.com:15001,br.smartproxy.com:15001,ar.smartproxy.com:15001

# --- Настройки Telegram --- 
BOT_TOKEN=
CHAT_ID=

# Telegram Bot конфигурация
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Настройки парсинга --- 
MAX_PAGES_TO_PARSE=1  # Максимальное количество страниц для парсинга
MAX_LISTINGS_TO_PARSE=10  # Максимальное количество объявлений для парсинга
MAX_LISTINGS_TO_SEND=5  # Максимальное количество объявлений для отправки

# --- Пути к файлам ---
SEEN_LISTINGS_FILE=data/seen_listings.json  # Файл для хранения уже обработанных объявлений
LISTINGS_DATA_FILE=data/listings.json  # Файл для хранения данных объявлений
ERROR_SCREENSHOTS_DIR=errors/  # Директория для скриншотов ошибок

# --- Настройки логирования ---
LOG_LEVEL=INFO  # Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_FILE=logs/parser.log  # Файл для записи логов
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s  # Формат записи логов 